bootJar { enabled = false }
jar { enabled = true }

dependencies {
    // db-module 프로젝트를 가져와 사용함
    implementation project(':common-data')

    // 비즈니스 로직에 필요한 추가 라이브러리 (예: 검증)
    implementation 'org.springframework.boot:spring-boot-starter-validation'
}